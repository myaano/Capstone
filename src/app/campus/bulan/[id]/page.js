import Header from "../../reusable_components/Header";

// render each of the paper based of params from /campus/bulan where each papers are rendered similar to /thesis

export default async function BulanPapers({ params }) {
  const { paperID } = await params;
  return (
    <>
      <div></div>
    </>
  );
}
